version: "3.9"

services:
  verifying-paymaster:
    container_name: verifying-paymaster-optimism-goerli-v1.2
    build: .
    restart: unless-stopped
    ports:
      - 10206:8080
    volumes:
      - ./paymaster/configs/.env-optimism-goerli:/app/paymaster/.env
    env_file:
      - .env
    environment:
      - TZ=Asia/Shanghai
