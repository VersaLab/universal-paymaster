version: "3.9"

services:
  universal-paymaster:
    container_name: universal-paymaster-optimism-goerli-v1.2
    build: .
    restart: unless-stopped
    ports:
      - 10206:8080
    volumes:
      - ./paymaster/configs/.env-optimism-goerli:/app/paymaster/.env
    env_file:
      - .env
    environment:
      - TZ=Asia/Shanghai
